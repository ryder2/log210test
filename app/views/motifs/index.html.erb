<p id="notice"><%= notice %></p>

<h1>Motifs</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @motifs.each do |motif| %>
      <tr>
        <td><%= motif.nom %></td>
        <td><%= button_to 'Supprimer', motif, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Créer', new_motif_path %> |
<%= link_to 'Retour au menu des demandes', demandes_path %>