<p id="notice"><%= notice %></p>

<h1>Listing Motifs</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @motifs.each do |motif| %>
      <tr>
        <td><%= motif.nom %></td>
        <td><%= button_to 'Destroy', motif, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Motif', new_motif_path %> |
<%= link_to 'Retour au menu des demandes', demandes_path %>